name: Extract label evidence with Robotoff

on:
  workflow_run:
    workflows: [Source sync]
    types: [completed]
  workflow_dispatch:
    inputs:
      source_run_id:
        description: Successful Source sync workflow run ID to inspect
        required: true
        type: string
      expected_input_hash:
        description: Optional source-export SHA-256 to pin for a manual run
        required: false
        type: string

permissions:
  actions: read
  contents: read

concurrency:
  group: open-food-facts-robotoff-extraction
  cancel-in-progress: false

jobs:
  extract:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 240
    env:
      SOURCE_RUN_ID: ${{ github.event.workflow_run.id || inputs.source_run_id }}
      EXPECTED_INPUT_HASH: ${{ inputs.expected_input_hash || '' }}
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Download source-complete snapshot
        uses: actions/github-script@v9
        env:
          RUN_ID: ${{ env.SOURCE_RUN_ID }}
        with:
          script: |
            const fs = require('fs');
            const runId = Number(process.env.RUN_ID);
            if (!Number.isSafeInteger(runId) || runId <= 0) throw new Error('source_run_id must be a positive workflow run ID');
            const response = await github.rest.actions.listWorkflowRunArtifacts({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: runId,
              per_page: 100,
            });
            const expectedName = `open-food-facts-snapshot-${runId}`;
            const artifact = response.data.artifacts.find((candidate) => candidate.name === expectedName && !candidate.expired);
            if (!artifact) throw new Error(`Source artifact ${expectedName} was not found or has expired`);
            const download = await github.rest.actions.downloadArtifact({
              owner: context.repo.owner,
              repo: context.repo.repo,
              artifact_id: artifact.id,
              archive_format: 'zip',
            });
            fs.writeFileSync('.data-source.zip', Buffer.from(download.data));

      - name: Expand and validate source snapshot
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .data/source-snapshot
          unzip -q .data-source.zip -d .data/source-snapshot
          cd .data/source-snapshot
          sha256sum --check checksums.sha256
          node --input-type=module <<'NODE'
          import { readFile } from 'node:fs/promises';
          const manifest = JSON.parse(await readFile('manifest.json', 'utf8'));
          const failures = [];
          if (manifest.source !== 'open_food_facts') failures.push('unexpected source');
          if (manifest.mode !== 'production') failures.push('source mode is not production');
          if (manifest.sourceComplete !== true) failures.push('source is not complete');
          if (manifest.terminalEvidence !== 'end_of_file') failures.push('source did not reach end-of-file');
          if (process.env.EXPECTED_INPUT_HASH && manifest.inputHash !== process.env.EXPECTED_INPUT_HASH) failures.push('input hash differs from the requested hash');
          if (failures.length) throw new Error(failures.join('; '));
          NODE

      - name: Extract every eligible nutrition label image
        shell: bash
        run: |
          set -uo pipefail
          status=1
          for pass in 1 2 3 4; do
            echo "Starting Robotoff pass $pass of 4"
            set +e
            pnpm data:extract -- \
              --source robotoff \
              --input .data/source-snapshot/staged-products.jsonl \
              --manifest .data/source-snapshot/manifest.json \
              --output .data/robotoff \
              --mode production \
              --confidence-threshold 0.85 \
              --minimum-interval-ms 1100
            status=$?
            set -e
            if [[ "$status" -eq 0 ]]; then
              break
            fi
            if [[ "$pass" -lt 4 ]]; then
              echo "Extraction remains incomplete; cooling down before resuming failed barcodes"
              sleep 60
            fi
          done
          exit "$status"

      - name: Verify terminal barcode accounting
        shell: bash
        run: |
          set -euo pipefail
          node --input-type=module <<'NODE'
          import { readFile } from 'node:fs/promises';
          const manifest = JSON.parse(await readFile('.data/robotoff/manifest.json', 'utf8'));
          const report = JSON.parse(await readFile('.data/robotoff/report.json', 'utf8'));
          const failures = [];
          if (manifest.source !== 'open_food_facts_robotoff') failures.push('unexpected extraction source');
          if (manifest.mode !== 'production') failures.push('extraction mode is not production');
          if (manifest.sourceComplete !== true || report.sourceComplete !== true) failures.push('extraction is incomplete');
          if (manifest.terminalEvidence !== 'end_of_file') failures.push('extraction did not exhaust the eligible barcode set');
          if (report.accountedBarcodes !== report.requestedBarcodes) failures.push('barcode accounting does not reconcile');
          if (report.outcomes?.failed !== 0) failures.push('extraction contains failed barcodes');
          if (failures.length) throw new Error(failures.join('; '));
          NODE

      - name: Record extraction checksums
        shell: bash
        run: |
          set -euo pipefail
          cd .data/robotoff
          sha256sum \
            manifest.json \
            report.json \
            source-index.jsonl \
            exclusions.jsonl \
            outcomes.jsonl \
            staged-products.jsonl \
            > checksums.sha256
          find responses -type f -name '*.json' ! -name '*.error.json' -print0 | sort -z | xargs -0 sha256sum >> checksums.sha256

      - name: Upload review-gated label candidates
        uses: actions/upload-artifact@v7
        with:
          name: robotoff-label-candidates-${{ github.run_id }}
          compression-level: 9
          retention-days: 30
          if-no-files-found: error
          path: |
            .data/robotoff/manifest.json
            .data/robotoff/report.json
            .data/robotoff/source-index.jsonl
            .data/robotoff/exclusions.jsonl
            .data/robotoff/outcomes.jsonl
            .data/robotoff/staged-products.jsonl
            .data/robotoff/responses/*.json
            .data/robotoff/checksums.sha256

      - name: Upload extraction diagnostics
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: robotoff-label-diagnostics-${{ github.run_id }}
          retention-days: 14
          if-no-files-found: warn
          path: |
            .data/robotoff/manifest.json
            .data/robotoff/report.json
            .data/robotoff/outcomes.jsonl
            .data/robotoff/responses/*.error.json
